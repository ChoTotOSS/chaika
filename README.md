# chaika

Usage:

    Usage of chaika
    -consul-host string
        Consul hostname (default "localhost")
    -consul-port int
        Consul port (default 8500)
    -p int
        Port for agent run on (default 2435)
